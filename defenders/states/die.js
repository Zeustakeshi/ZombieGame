import { Die, states } from "../../state.js";

export default class DieDefender extends Die {
    constructor(character, maxFrameX, maxFrameY) {
        super(character, maxFrameX, maxFrameY);
    }
}
